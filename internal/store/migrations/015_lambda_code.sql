-- Lambda code metadata (code_sha256, code_size, blob_key) is persisted
-- in the jc_resources JSONB data column alongside other function fields.
-- No schema changes are required for the generic resource store.
SELECT 1;
