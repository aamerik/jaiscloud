---
# PersistentVolumeClaim — S3 blob bytes survive pod restarts
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jaiscloud-blobs-pvc
  namespace: jaiscloud
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
---
# ConfigMap — non-secret runtime settings
apiVersion: v1
kind: ConfigMap
metadata:
  name: jaiscloud-config
  namespace: jaiscloud
data:
  JAISCLOUD_PORT: "4566"
  JAISCLOUD_MODE: "full"
  JAISCLOUD_REGION: "us-east-1"
  JAISCLOUD_ACCOUNT_ID: "000000000000"
  JAISCLOUD_LOG_LEVEL: "info"
  JAISCLOUD_METRICS: "true"
  JAISCLOUD_BLOB_DIR: "/data/blobs"
  JAISCLOUD_EXECUTOR_MODE: "k8s"
  JAISCLOUD_SPARK_IMAGE: "apache/spark:3.5.0"
  JAISCLOUD_LAMBDA_IMAGE: "public.ecr.aws/lambda/python:3.12"
  JAISCLOUD_K8S_NAMESPACE: "jaiscloud"
  JAISCLOUD_K8S_APISERVER: "https://kubernetes.default.svc"
  JAISCLOUD_K8S_SA: "jaiscloud"
  JAISCLOUD_K8S_CA_FILE: "/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
  JAISCLOUD_K8S_TOKEN_FILE: "/var/run/secrets/kubernetes.io/serviceaccount/token"
---
# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jaiscloud
  namespace: jaiscloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jaiscloud
  template:
    metadata:
      labels:
        app: jaiscloud
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "4566"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: jaiscloud
      terminationGracePeriodSeconds: 45
      containers:
        - name: jaiscloud
          image: jaiscloud:latest
          # Use local image built by deploy.sh — never pull from a registry
          imagePullPolicy: Never
          ports:
            - containerPort: 4566
          envFrom:
            - configMapRef:
                name: jaiscloud-config
          env:
            - name: JAISCLOUD_DSN
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: dsn
          volumeMounts:
            - name: blobs
              mountPath: /data/blobs
          readinessProbe:
            httpGet:
              path: /_jaiscloud/health
              port: 4566
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /_jaiscloud/health
              port: 4566
            initialDelaySeconds: 15
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: "1"
              memory: 512Mi
      volumes:
        - name: blobs
          persistentVolumeClaim:
            claimName: jaiscloud-blobs-pvc
---
# Service — LoadBalancer so `localhost:4566` works on Docker Desktop.
# Docker Desktop's built-in cloud-provider controller binds the external port
# directly to localhost, so no kubectl port-forward is needed.
# On other local clusters (minikube, kind) run:
#   kubectl port-forward -n jaiscloud svc/jaiscloud 4566:4566
apiVersion: v1
kind: Service
metadata:
  name: jaiscloud
  namespace: jaiscloud
spec:
  type: LoadBalancer
  selector:
    app: jaiscloud
  ports:
    - port: 4566
      targetPort: 4566
